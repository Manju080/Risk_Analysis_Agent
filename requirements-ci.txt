# Minimal dependencies for CI unit tests only
# Does NOT include torch/sentence-transformers/streamlit (too heavy for CI)

# Core API
fastapi>=0.115.0
uvicorn>=0.30.6
python-dotenv>=1.0.1
pydantic>=2.8.2
pydantic-settings>=2.10.1

# LangChain (core only — no HuggingFace/embedder)
langchain>=0.3.1
langchain-core>=0.3.6
langchain-community>=0.3.1

# Finance / Data (needed by risk_agent tests)
yfinance>=0.2.44
pandas>=2.2.2
numpy>=1.26.4
scipy>=1.14.1

# SQLite memory (needed by memory_agent import)
SQLAlchemy>=2.0.0

# Testing
pytest>=8.3.3
pytest-asyncio>=0.24.0
